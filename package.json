{
  "name": "beger-backend",
  "version": "1.0.0",
  "description": "Beger Backend API",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "drizzle-orm": "^0.44.5",
    "hono": "^4.9.6",
    "jose": "^6.1.0",
    "pino": "^9.9.4",
    "pino-pretty": "^13.1.1",
    "valibot": "^1.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.3",
    "@types/bun": "^1.2.21",
    "cpy-cli": "^6.0.0",
    "drizzle-kit": "^0.31.4",
    "oxlint": "^1.14.0"
  },
  "scripts": {
    "build": "NODE_ENV=production bun build.script.ts",
    "build:dev": "NODE_ENV=development bun build.script.ts",
    "dev": "NODE_ENV=development bun --watch src/index.ts",
    "start": "NODE_ENV=production bun dist/index.mjs",
    "test": "NODE_ENV=test bun test",
    "lint": "oxlint . --fix",
    "format": "biome format --write .",
    "prebuild": "rm -rf dist && cpy certs dist",
    "docker:build": "DOCKER_BUILDKIT=1 docker build -t beger-backend:latest .",
    "docker:run": "docker run -d --name beger-backend -p 5333:5333 --env-file .env beger-backend:latest",
    "docker:stop": "docker stop beger-backend && docker rm beger-backend",
    "compose:build": "docker-compose build",
    "compose:up": "docker-compose up -d",
    "compose:down": "docker-compose down",
    "compose:logs": "docker-compose logs -f",
    "compose:restart": "docker-compose restart",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:migrate": "drizzle-kit migrate",
    "db:seed:dev": "NODE_ENV=development bun src/db/seed.script.ts",
    "db:seed:prod": "NODE_ENV=production bun dist/seed.script.mjs",
    "db:reset": "drizzle-kit drop && bun db:push && bun db:seed:dev"
  },
  "license": "MIT"
}