{
	"name": "beger-backend",
	"version": "1.0.0",
	"description": "Beger Backend API",
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"license": "MIT",
	"dependencies": {
		"@hono/valibot-validator": "^0.5.3",
		"drizzle-orm": "^0.44.6",
		"hono": "^4.9.9",
		"memcached": "^2.2.2",
		"pino": "^9.12.0",
		"postgres": "^3.4.7",
		"drizzle-kit": "^0.31.5",
		"valibot": "^1.1.0"
	},
	"devDependencies": {
		"@hono/swagger-ui": "^0.5.2",
		"@biomejs/biome": "^2.2.5",
		"@types/bun": "^1.2.23",
		"@types/memcached": "^2.2.10",
		"cpy-cli": "^6.0.0",
		"cross-env": "^10.1.0",
		"oxlint": "^1.19.0",
		"pino-pretty": "^13.1.1",
		"rimraf": "^6.0.1",
		"typescript": "^5.9.3"
	},
	"scripts": {
		"build:dev": "cross-env NODE_ENV=development bun scripts/build.script.ts",
		"build:prod": "cross-env NODE_ENV=production bun scripts/build.script.ts",
		"start:dev": "cross-env NODE_ENV=development bun --watch src/index.ts",
		"start:prod": "cross-env NODE_ENV=production bun dist/index.mjs",
		"start:prod:dev": "cross-env NODE_ENV=development bun dist/index.mjs",
		"test": "bun test",
		"lint": "oxlint . --fix",
		"format": "biome format --write .",
		"prebuild:dev": "rimraf dist && cpy certs dist",
		"prebuild:prod": "rimraf dist && cpy certs dist",
		"docker:build": "DOCKER_BUILDKIT=1 docker build -t beger-backend:latest .",
		"docker:run": "docker run -d --name beger-backend -p 5333:5333 --env-file .env beger-backend:latest",
		"docker:stop": "docker stop beger-backend && docker rm beger-backend",
		"db:generate": "drizzle-kit generate",
		"db:push": "drizzle-kit push",
		"db:migrate": "drizzle-kit migrate",
		"db:seed:dev": "cross-env NODE_ENV=development bun scripts/seed.script.ts",
		"db:seed:prod": "cross-env NODE_ENV=production bun dist/seed.script.mjs",
		"db:reset": "drizzle-kit drop && bun db:push && bun db:seed:dev"
	}
}
